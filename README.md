# Broadcaster FS Update Feed

This repository hosts the public updater manifest for Broadcaster FS.

- `latest.json` is the manifest the desktop app checks.
- Release assets are attached to GitHub Releases and referenced from the manifest.

